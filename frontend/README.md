# Ragverse Frontend

React + Vite frontend for Ragverse.

## Local Development

```bash
npm install
npm run dev
```

Open `http://localhost:5173`.

Set `VITE_API_URL` in `.env` when the FastAPI backend is not running at `http://localhost:8000/api`.
